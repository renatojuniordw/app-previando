# 06 — AI
> Arquitetura de modelos OpenAI — hierarquia por criticidade
> Última atualização: 2026-07-22

---

## Princípios

1. IA nunca toma decisões jurídicas — apenas estrutura e formata
2. Output sempre requer revisão do advogado
3. Temperatura 0 para extração, 0.3 para geração de texto
4. Input sempre sanitizado com `sanitizeForAI()` antes do prompt
5. Custo monitorado por geração — tokens e USD logados
6. Modelo centralizado em `src/lib/ai-models.ts`
7. Todo prompt salvo em `opinions.promptUsed` para auditoria
8. Rodapé "Previando" em todo parecer gerado
9. **Anti-prompt-injection** aprimorado na sanitização

---

## Constantes de Modelos

```typescript
// src/lib/ai-models.ts
const AI_MODELS_MAP = {
  CRITICAL:    'gpt-4.1-mini',  // tarefas jurídicas e extração previdenciária
  OPERATIONAL: 'gpt-4.1-nano',  // tarefas simples de texto
} as const

export const AI_MODELS = AI_MODELS_MAP
export const AI_MAX_TOKENS: number = 16384

export const AI_COST_PER_TOKEN: Record<string, number> = {
  'gpt-4.1-mini': 0.0000008,
  'gpt-4.1-nano':  0.0000002,
}
```

### Hierarquia

| Nível | Constante | Modelo | Quando usar |
|---|---|---|---|
| CRITICAL | `AI_MODELS.CRITICAL` | `gpt-4.1-mini` | CNIS full parse, pareceres, diagnóstico |
| OPERATIONAL | `AI_MODELS.OPERATIONAL` | `gpt-4.1-nano` | Validação CNIS, classificação |

---

## Cliente OpenAI

```typescript
// src/lib/openai.ts
let _openai: OpenAI | null = null

export function getOpenAI(): OpenAI { /* lazy singleton */ }
export const openai = new Proxy({} as OpenAI, { /* proxy pattern */ })
```

| Parâmetro | Valor | Justificativa |
|---|---|---|
| `timeout` | 180 000ms (3 min) | CNIS de 20+ páginas |
| `maxRetries` | 3 | Tolerância a falhas |

### loggingFetch
O cliente OpenAI agora utiliza um wrapper `loggingFetch` que:
- Loga requisições e respostas da API OpenAI
- Facilita depuração de timeouts e erros
- Preserva o comportamento de timeout de 180s

---

## Sanitização de Input

```typescript
// src/lib/sanitize.ts
export function sanitizeForAI(input: string, maxLength: number = 3000): string
// Remove: ---, ```, prompt injection, caracteres de controle
// Preserva: tabs, newlines
// Trunca: maxLength (default 3000)
```

### Melhorias Anti-Prompt-Injection
- **Bloqueio de instruções injetadas:** Detecta e remove padrões como "ignore as instruções anteriores", "system prompt override"
- **Remoção de delimitadores de sistema:** Remove `[SYSTEM]`, `<|im_start|>`, `<<SYS>>` e tokens similares
- **Sanitização de tokens de controle:** Remove tokens especiais do modelo que poderiam alterar o comportamento
- **Normalização de variáveis de ambiente:** Substitui `$VARIABLE` patterns por placeholders seguros
- **Escaping de aspas e backticks:** Previne quebra de contexto em templates de prompt

---

## Tabela de Uso por Funcionalidade

### Operações com `AI_MODELS.*`

| Operação | Modelo | Temp | Max Tokens | Arquivo |
|---|---|---|---|---|
| CNIS Full Parse | gpt-4.1-mini (CRITICAL) | 0 | 16384 | `services/cnis/ai-parser.ts` |
| CNIS Validation | gpt-4.1-nano (OPERATIONAL) | 0 | 100 | `services/cnis/ai-parser.ts` |
| Parecer Jurídico | gpt-4.1-mini (CRITICAL) | 0.3 | 16384 | `services/opinion-generator.ts` |
| Diagnóstico Prontuário | gpt-4.1-mini (CRITICAL) | 0.3 | 16384 | `api/.../notes/diagnosis/route.ts` |
| Petição Inicial IA | gpt-4.1-mini (CRITICAL) | 0.3 | 16384 | `services/peticao-generator.ts` |

### Operações BPC (modelo hardcoded gpt-4o-mini)

| Operação | Modelo | Temp | Max Tokens | Arquivo |
|---|---|---|---|---|
| BPC Pre-Análise | gpt-4o-mini | 0.3 | 3000 | `services/bpc/index.ts` |
| BPC Laudo | gpt-4o-mini | 0.3 | 3000 | `services/bpc/index.ts` |
| BPC Perguntas Sociais | gpt-4o-mini | 0.3 | 3000 | `services/bpc/index.ts` |
| BPC Perguntas Médicas | gpt-4o-mini | 0.3 | 3000 | `services/bpc/index.ts` |
| BPC Checklist | gpt-4o-mini | 0.3 | 1500 | `services/bpc/index.ts` |

> **Nota:** `gerarCarrossel` foi removido do serviço BPC. O carrossel para redes sociais não é mais gerado via IA no backend.

---

## Desvios Conhecidos

### `gpt-4o-mini` hardcoded no BPC

O serviço BPC (`src/services/bpc/index.ts`) usa `gpt-4o-mini` diretamente hardcoded em cada função. Isso viola a regra "modelo nunca hardcoded".

**Justificativa:** O BPC é um módulo experimental com prompts especializados e volumes baixos.

**Risco:** Se o modelo for descontinuado, todas as funções do BPC precisam ser atualizadas manualmente.

**TODO:** Criar `AI_MODELS.BPC` ou `AI_MODELS.LIGHTWEIGHT` e migrar o BPC (continua pendente).

---

## Detalhamento das Operações

### CNIS Full Parse
- **Modelo:** gpt-4.1-mini, **Temp:** 0
- **Max Tokens:** 16384, **Timeout:** 180s
- **System prompt:** Especialista em Direito Previdenciário
- **Fallback JSON:** regex `/\{[\s\S]*\}/`

### CNIS Validation
- **Modelo:** gpt-4.1-nano, **Temp:** 0
- **Input:** head (8000 chars) + tail (3000 chars)
- **Output:** `{"valid": true}` ou `{"valid": false, "reason": "..."}`

### Geração de Parecer
- **Modelo:** gpt-4.1-mini, **Temp:** 0.3
- **Regras:** max 4 parágrafos, disclaimer obrigatório, nunca citar leis sem certeza

---

## CNIS Worker — Limite de Páginas

- O worker `cnis-processing` agora possui um limite de **200 páginas** (`max: 200`)
- PDFs com mais de 200 páginas são rejeitados com erro antes do processamento
- Anteriormente o valor era `0` (ilimitado), o que causava timeouts e consumo excessivo de tokens
- O limite é verificado antes de qualquer processamento (antes mesmo do OCR)

---

## Rate Limiting

| Operação | Limite | Janela | Redis Key |
|---|---|---|---|---|
| Pareceres IA | 20/hora | 3600s | `ai:{userId}` |
| Diagnósticos | 10/hora | 3600s | `diagnosis:{userId}` |
| BPC (formulário) | 10/hora | 3600s | `bpc:{userId}` |
| BPC (análises IA) | 15/hora | 3600s | `bpc:{userId}` |
| Sugestão modalidades | 10/min | 60s | `suggest-modalities:{userId}` |

---

## Controle de Custo

| Operação | Tokens | Custo USD |
|---|---|---|
| Parse CNIS (IA) | ~20.000 | ~$0,016 |
| Parecer | ~3.000 | ~$0,0024 |
| Diagnóstico | ~2.000 | ~$0,0016 |

### Projeção Mensal (50 usuários)
- CNIS: ~$4,00/mês
- Pareceres: ~$2,40/mês
- **Total: ~$7,00/mês**

---

## Regras Globais

1. **Temperatura:** 0 (extração), 0.3 (texto), 0.5 (criatividade, legado carrossel)
2. **Max tokens:** Sempre definido — nunca aberto
3. **Timeout:** 180s
4. **Retries:** 3 tentativas automáticas
5. **Sanitização:** Todo input passa por `sanitizeForAI()` com proteção anti-prompt-injection
6. **Auditoria:** Todo prompt salvo em `opinions.promptUsed`
7. **Custo:** Salvo em `opinions.generationCostUsd` e `opinions.tokensUsed`
8. **Revisão obrigatória:** Advogado sempre revisa
9. **Rodapé:** "Calculado via Previando (app.previando.com.br)"
10. **Modelo centralizado:** Via `AI_MODELS.*` — exceção: BPC usa `gpt-4o-mini`
